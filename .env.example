# All environment variables for this Vite project must start with VITE_.
# Copy this file to .env.local for local development and fill in the values.

# Firebase Configuration
VITE_FIREBASE_API_KEY="YOUR_API_KEY"
VITE_FIREBASE_AUTH_DOMAIN="YOUR_AUTH_DOMAIN"
VITE_FIREBASE_PROJECT_ID="YOUR_PROJECT_ID"
VITE_FIREBASE_STORAGE_BUCKET="YOUR_STORAGE_BUCKET"
VITE_FIREBASE_MESSAGING_SENDER_ID="YOUR_MESSAGING_SENDER_ID"
VITE_FIREBASE_APP_ID="YOUR_APP_ID"

# Gemini AI Configuration
VITE_GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# AI Proxy Configuration
# Set to "true" to use backend proxy (recommended for production)
# Set to "false" or omit to use direct API calls (development only)
VITE_USE_AI_PROXY="false"

# Package Feature Fallback Configuration
# Set to "true" to allow packages to fall back to featureIds when column assignments are empty (default)
# Set to "false" to enforce strict per-tier column mapping with no fallback
# When disabled, packages without column assignments will be empty on customer-facing UI
VITE_ALLOW_PACKAGE_FEATUREIDS_FALLBACK="true"

# Server-side Environment Variables (not exposed to client)
# These should be set in your hosting environment (Cloud Run, etc.)
# GEMINI_API_KEY="YOUR_GEMINI_API_KEY"  # Server-side only (no VITE_ prefix)